This is a prompt injection issue. The agent is completing the flow at 4 fields and never reaching the subtopic step. The root cause is in Rule #0.25 and Section 4 — the "all 4 filled → show summary" logic fires immediately without going through the subtopic gate first.
The fix needs to happen in 3 specific places:

Fix 1 — Rule #0.25 (The most critical fix)
Find this exact block:
2. **IF ALL 4 ARE FILLED (all non-empty):**
   - ✅ Show ALL 4 fields in message with ✓ checkmarks:
   ...
   - ✅ Set `dropdown: {}` (EMPTY - NO dropdowns!)
   - ✅ Set `isMetadataExtractionDone: false` (waiting for confirmation)
   - ✅ Ask: "Please confirm if everything is correct..."
   - ❌ **NEVER show any dropdown when all 4 are filled!**
   - ❌ **NEVER ask user to "select a topic" or any field when all 4 are filled!**
   - ❌ **NEVER skip showing any of the 4 fields in the message!**
Replace with:
2. **IF ALL 4 MANDATORY FIELDS ARE FILLED (all non-empty):**

   🚨 DO NOT GO TO SUMMARY YET. SUBTOPIC STEP MUST HAPPEN FIRST.

   SUBTOPIC GATE (MANDATORY - CHECK THIS BEFORE SUMMARY):
   ├── Has subtopic already been asked in this conversation?
   │   ├── NO → Ask for subtopic NOW before showing summary
   │   │   - Message: "Please select a subtopic, or say **skip** 
   │   │     if you'd like to proceed without one."
   │   │   - Show ONLY dropdown.subtopic (dynamically injected list)
   │   │   - Set isMetadataExtractionDone: false
   │   │   - DO NOT show summary yet
   │   │
   │   └── YES (subtopic was asked) →
   │       ├── User selected a value → subtopic ≠ "" → Proceed to summary
   │       └── User skipped → subtopic = "" → Proceed to summary
   │
   ONLY AFTER SUBTOPIC IS RESOLVED (selected OR skipped):
   - ✅ Show ALL 4 mandatory fields in message with ✓ checkmarks
   - ✅ Show ✓ Subtopic: [value] ONLY if subtopic ≠ ""
   - ✅ Omit subtopic line entirely if subtopic = "" (was skipped)
   - ✅ Set dropdown: {} (EMPTY - no dropdowns during summary)
   - ✅ Set isMetadataExtractionDone: false (waiting for confirmation)
   - ✅ Ask: "Please confirm if everything is correct..."

   ❌ NEVER jump to summary when subtopic has not been asked yet
   ❌ NEVER show subtopic dropdown during summary
   ❌ NEVER show "✓ Subtopic: Not selected" or any placeholder

Fix 2 — Section 4, Mode 1: Step-by-Step Collection
Find:
5. When all 4 filled → Show summary + ask for confirmation
Replace with:
5. Ask for subtopic → subtopic dropdown
   Message: "Please select a subtopic, or say **skip** if you'd 
   like to proceed without one."
   → If user selects a value  → set subtopic: [value], go to step 6
   → If user skips            → set subtopic: "", go to step 6

6. Show summary + ask for confirmation
   → Include ✓ Subtopic: [value] ONLY if subtopic ≠ ""
   → Omit subtopic line entirely if subtopic was skipped

Fix 3 — Section 4, Mode 2: Step 5 (STEP 5.0 Field Completion)
Find:
IF all 4 are non-empty → ALL 4 FIELDS FILLED → Go to "all 4 filled" branch
Replace with:
IF all 4 are non-empty → ALL 4 MANDATORY FIELDS FILLED →
   Check subtopic gate first:
   → Subtopic not yet asked → Go to "ask subtopic" branch
   → Subtopic already resolved → Go to "show summary" branch
IF 1-3 are filled → SOME FIELDS FILLED → Go to "some fields filled" branch
IF 0 are filled   → NO FIELDS FILLED   → Go to "no fields filled" branch

Why these 3 fixes together solve it
Right now the flow short-circuits at step 4:
BEFORE (broken):
4 fields filled → Rule #0.25 fires → Summary shown → subtopic never asked

AFTER (fixed):
4 fields filled → Subtopic gate in Rule #0.25 → Ask subtopic → 
User selects or skips → Summary shown
The key insight is that Rule #0.25 is the most dominant rule in this prompt — it fires before Section 4's flow logic. That's why the Section 4 change alone wasn't enough. The fix in Rule #0.25 is the one that will actually stop the agent from jumping to summary prematurely.